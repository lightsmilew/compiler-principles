# ==========================================================================
# 编译原理实验文档站 · Git 忽略规则
# 原则：只提交「内容与配置」——docs/ 下的 Markdown、站点源码与配置、CI 工作流。
#       所有可再生成的产物、本地环境文件、编辑器与系统垃圾一律不入库。
# ==========================================================================

# --------------------------------------------------------------------------
# 依赖
# --------------------------------------------------------------------------
/node_modules
/.pnp
.pnp.*
.yarn/cache
.yarn/install-state.gz
.yarn/unplugged
.yarn/build-state.yml

# --------------------------------------------------------------------------
# 构建产物（可由 npm run build 重新生成，绝不入库）
# --------------------------------------------------------------------------
/build
/.docusaurus
/.cache-loader
.cache/
*.tsbuildinfo

# --------------------------------------------------------------------------
# 环境变量（可能含密钥，只保留不含敏感值的示例文件）
# --------------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
!.env.example

# --------------------------------------------------------------------------
# 日志与调试输出
# --------------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.lerna-debug.log*
.electron-builder.log

# --------------------------------------------------------------------------
# 编译产物
#   与本课程提交规范保持一致（实验报告规范中明确禁止提交这类文件）
# --------------------------------------------------------------------------
*.o
*.obj
*.a
*.so
*.dylib
*.dll
*.exe
*.class
*.jar
*.pyc
a.out
__pycache__/
/venv
/.venv

# --------------------------------------------------------------------------
# 覆盖率与测试产物
# --------------------------------------------------------------------------
/coverage
/.nyc_output
*.lcov

# --------------------------------------------------------------------------
# 编辑器与 IDE
#   如需共享推荐扩展或格式化配置，可单独强制添加：
#   git add -f .vscode/extensions.json
# --------------------------------------------------------------------------
.idea/
/.vscode/*
!/.vscode/extensions.json
*.iml
*.sublime-project
*.sublime-workspace
/.fleet
.history/
*.swp
*.swo
*~

# --------------------------------------------------------------------------
# 操作系统
# --------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# --------------------------------------------------------------------------
# 临时文件
# --------------------------------------------------------------------------
*.tmp
*.temp
*.bak
*.orig
*.rej
/tmp/
